{
  "name": "@{{{pkg 'name'}}}/{{{dashCase name}}}",
  "version": "{{{pkg 'version'}}}",
  "description": "{{{description}}}",
  "main": "src/{{{properCase name}}}.tsx",
  "sass": "assets/{{{properCase name}}}.scss",
  "publishConfig": {
    "main": "dist/{{{properCase name}}}.js",
    "typings": "dist/{{{properCase name}}}.d.ts"
  },
  "files": [
    "/assets",
    "/dist"
  ],
  "scripts": {
    "test": "jest",
    "prepublishOnly": "npm run clean && npm run build",
    "build": "tsc",
    "clean": "rm -rf dist tsconfig.tsbuildinfo"
  },
  "author": "{{{pkg 'author'}}}",
  "license": "{{{pkg 'license'}}}",
  "keywords": [
    "react-components"
  ],
  "dependencies": {
    "@not-govuk/component-helpers": "{{#if (eq (pkg 'name')'not-govuk')}}workspace:{{/if}}^0.3.0"
  },
  "peerDependencies": {
    "@not-govuk/docs-components": "^0.3.0",
    "react": "^16.9.55"
  },
  "devDependencies": {
    "@mdx-js/react": "^1.6.5",
    "@not-govuk/component-test-helpers": "{{#if (eq (pkg 'name')'not-govuk')}}workspace:{{/if}}^0.3.0",
    "@types/react": "^16.9.55",
    "jest": "^26.1.0",
    "ts-jest": "^26.1.1",
    "typescript": "^3.9.2"
  }
}
