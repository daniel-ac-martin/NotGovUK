import { Meta, Preview, Props, Story } from "@storybook/addon-docs/blocks";
import { boolean, text } from "@storybook/addon-knobs";
import { Footer } from '../../';
import readMe from './README.md';

<Meta
  title="Components/Footer"
  component={Footer}
  parameters={{
    jest: ['footer'],
    notes: readMe
  }}
/>

# Footer

<Preview withToolbar>
  <Story name="Footer">
    <Footer
      className={text('className')}
      id={text('id')}
      wide={boolean('wide')}
    >
      {text('children', 'My custom footer.')}
    </Footer>
  </Story>
</Preview>

<Props of={Footer} />


## How it works

<Preview>
  <Story name="Standard">
    <Footer />
  </Story>
</Preview>


### Wide

EXPERIMENTAL

A `wide` flag can be provided for when you do NOT wish to restrict the width on wide screens.

<Preview>
  <Story name="Wide">
    <Footer
      wide
    />
  </Story>
</Preview>