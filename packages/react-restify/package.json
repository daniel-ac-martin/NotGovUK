{
  "name": "@not-govuk/react-restify",
  "version": "0.1.0",
  "description": "A customised Restify server for isomorphic React applications.",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepare": "pnpm run build",
    "build": "tsc -b tsconfig.build.json",
    "clean": "rm -rf dist tsconfig.build.tsbuildinfo"
  },
  "keywords": [
    "restify"
  ],
  "author": "Daniel A.C. Martin <npm@daniel-martin.co.uk> (http://daniel-martin.co.uk/)",
  "license": "ISC",
  "dependencies": {
    "bunyan": "^1.8.12",
    "restify": "^8.5.1",
    "restify-bunyan-logger": "^2.0.7",
    "restify-errors": "^8.0.2"
  },
  "devDependencies": {
    "@types/bunyan": "^1.8.6",
    "@types/restify": "^8.4.2",
    "typescript": "^3.8.3"
  },
  "files": [
    "dist"
  ]
}
